# Required in every deployed environment
DATABASE_URL="postgresql://quizforge:quizforge@localhost:5432/quizforge?schema=public"
JWT_SECRET="replace-with-at-least-32-random-characters"
# Must match the public site URL exactly (scheme + host, no trailing slash).
# Local: http://localhost:3000 | Production: https://quizora-online.vercel.app
APP_URL="http://localhost:3000"

# AI provider: keep server-side only. Never prefix with NEXT_PUBLIC_.
OPENROUTER_API_KEY=""
OPENROUTER_MODEL="openai/gpt-oss-120b:free"
OPENROUTER_FALLBACK_MODEL="meta-llama/llama-3.3-70b-instruct"
OPENROUTER_EVALUATION_MODEL="google/gemma-4-31b-it"
OPENROUTER_TIMEOUT_MS="55000"
OPENROUTER_EVALUATION_TIMEOUT_MS="12000"

# Transactional email via Nodemailer (SMTP).
# In development, leave SMTP unset to log verification links in the console.
# Gmail example: SMTP_HOST=smtp.gmail.com SMTP_PORT=587 SMTP_SECURE=false
# Use an app password for SMTP_PASS when your provider requires 2FA.
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
SMTP_SECURE="false"
EMAIL_FROM="QuizForge <you@gmail.com>"

# Optional one-time admin bootstrap values used only by npm run db:seed.
SEED_ADMIN_EMAIL=""
SEED_ADMIN_PASSWORD=""

# Optional deployment metadata sent to OpenRouter.
OPENROUTER_SITE_URL="http://localhost:3000"
OPENROUTER_APP_NAME="QuizForge"
